apiVersion: v1
kind: Service
metadata:
  name: python-backend-service
spec:
  selector:
    app: python-backend
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000

